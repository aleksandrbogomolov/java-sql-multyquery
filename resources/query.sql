select max(code || ' ' || fullname) from warehouse where id_controwner is not null;
SELECT status, count(*) FROM rpl_user.fde_vetrequest_queue WHERE status IN (1) and enqueue_time >= trunc(sysdate - 1) GROUP BY status